{
  "name": "@dont-trust-that-card/shared",
  "private": true,
  "version": "0.1.0",
  "description": "Shared types and pure game logic package",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.7",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
